import { RouteObject } from "react-router-dom";
import AllRole from "../pages/All";

const RoleRoutes: RouteObject[] = [
  {
    path: "all-Roles",
    element: <AllRole />,
  },
];

export default RoleRoutes;
